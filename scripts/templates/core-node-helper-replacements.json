[
	{
		"searchValue": "const express = require(\"express\");",
		"replaceValue": "const serveStatic = require(\"serve-static\");",
		"label": "node_helper.js static import"
	},
	{
		"searchValue": "\t\tapp.use(`/${this.name}`, express.static(`${this.path}/public`));",
		"replaceValue": "\t\tapp.use(`/${this.name}`, serveStatic(`${this.path}/public`));",
		"label": "node_helper.js static middleware"
	}
]
